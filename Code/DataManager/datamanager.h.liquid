{% include 'header' %}

#import <Foundation/Foundation.h>

// Interfaces
#import "{{ prefix }}{{ module_info.name }}DataManagerDelegate.h"

@protocol {{ prefix }}{{ module_info.name }}InteractorDelegate;

@interface {{ prefix }}{{ module_info.name }}DataManager : NSObject <{{ prefix }}{{ module_info.name }}DataManagerDelegate>

@property (nonatomic, strong) id<{{ prefix }}{{ module_info.name }}InteractorInput> interactor;

@end